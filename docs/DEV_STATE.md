# Состояние разработки (точка возобновления)

> Обновлено: 2026-08-24 (Фаза 10 закрыта — история арендаторов;
> Фаза 12, Фаза 9, Фаза 8, Фазы 5–6 и визуальный редизайн закрыты ранее).
> Файл — «где остановились», чтобы
> продолжить в новой сессии (в том числе другой моделью) без потери
> контекста. История решений — `docs/CHANGELOG.md`, статусы —
> `docs/ROADMAP.md`, решения — `docs/adr/` (текущий индекс —
> `docs/adr/README.md`, дошло до ADR-0028). Обязательно прочитать перед
> новой задачей — см. корневой `CLAUDE.md`.

**Важно, если в памяти/старых заметках сессии встретится «сервис не
собирает ПДн» (ADR-0017) — это устарело.** Актуальное решение — ровно
противоположное (ADR-0021, тот же день чуть позже): сервис собирает
персональные данные обеих сторон договора. Читайте `docs/adr/0021-*`,
не `0017-*`, для актуального состояния.

## Контекст последней сессии (важно понять, прежде чем продолжать)

### Где остановились — сессия 2026-08-24

**Фаза 10 закрыта без ADR и миграции.** Добавлен landlord-only
`GET /properties/:id/lease-history`: чужой объект скрывается за 404, в историю
попадают только завершённые договоры этого собственника. Для каждого договора
возвращаются период, фактическая дата окончания, email арендатора, месячная
аренда и агрегат финальных счетов — оплачено вовремя, оплачено позже срока,
не оплачено. Черновые счета не участвуют; `LeasePartyInfo` не читается.

На `/properties/[id]` текущие договоры отделены от секции «История
арендаторов». Завершённая аренда показывает контакт, фактический период,
ставку и сводку пунктуальности; строка ведёт в сохранённый договор. Если
текущего договора нет, завершённая история больше не блокирует создание
следующего.

Backend suite — **317/317** (27 наборов); backend `tsc --noEmit` и
`nest build`, frontend `tsc --noEmit` и `next build` чисты. Живой API на
synthetic landlord проверен: объект читается, пустая история отвечает 200.
Авторизованная браузерная проверка новой секции не выполнялась: отдельная
in-app сессия была без входа; публичный экран входа после перезапуска frontend
открывается штатно.

### Предыдущий закрытый этап — Фаза 12

**Фаза 12 закрыта по ADR-0028.** `GET /reports/summary` теперь возвращает
портфель всех объектов собственника: статус аренды, арендатора и месячную
аренду действующего договора, доход, долг, незакрытые заявки и согласованные,
но ещё не выставленные услуги. Объект без договора и объект только с чужим
договором остаются в выдаче свободными с нулевыми показателями.

Доход и задолженность объектов накапливаются прямо в существующем проходе по
договорам, в тех же ветках, что общие итоги отчёта; второй реализации денег
нет. Прощённая пеня по-прежнему считается исторической формулой отчёта и не
пересматривалась. Знак услуги централизован в `tenantShareForPayer`, на него
переведён прежний биллинг. Услуги «на подходе» фильтруются и по владельцу
объекта, и по `landlordId` договора заявки.

На `/reports` секция «По объектам» стоит первой, группирует объекты по городу,
показывает сводку, статус и показатели каждой строки. Активный договор при
аномальных дублях выбирается по наибольшей `startDate`. Пустое состояние ведёт
на `/properties`. Новых полей БД нет, миграция и `prisma generate` не нужны.

Backend suite — **313/313** (27 наборов); backend `tsc --noEmit` и
`nest build`, frontend `tsc --noEmit` чисты.

- Живой E2E по API Фазы 12 ревьюером ВЫПОЛНЕН, 20/20: в ответе появился блок
  `portfolio` с `totals` и `entries`; три объекта получили корректные статусы —
  сданный `rented` с заполненными арендатором и месячной арендой, объект без
  договора `vacant` с пустыми полями, объект с черновиком `pending`; выполняется
  `properties = rented + pending + vacant`; сумма доходов по объектам совпадает
  с `income.total`, сумма долгов — с `outstanding.totalDue`; открытые заявки
  считаются (4), деньги на подходе равны -1000 при плательщиках `tenant` 6000,
  `split` 4000 и `owner` 9000; после перевода заявки в `resolved` выставленная
  услуга уходит из суммы (остаётся -7000) и счётчик падает до 3; чужие объекты в
  портфель не попадают, у другого собственника ровно свой объект; города
  отсортированы. Throwaway-данные удалены.
- Браузерная проверка блока «По объектам» ВЫПОЛНЕНА: секция стоит первой на
  `/reports`, сводка показывает «Объектов 3 · сдано 1 · свободно 1 · ожидают
  арендатора 1» плюс пилюли «Заявок в работе 3» и «Деньги на подходе: −1 000 ₽»;
  список сгруппирован по городам (Ангарск, затем Иркутск); статусы различаются
  цветом и иконкой — «Свободен», «Ожидает арендатора», «Сдан»; у сданного
  объекта в подписи видны арендатор и месячная аренда; по объектам показаны
  Доход, Долг, Заявки и «На подходе» со знаком; существующие тематические секции
  ниже не изменились; подзаголовок страницы дополнен упоминанием портфеля.

### Предыдущий закрытый этап — Фаза 9

**Фаза 9 закрыта по ADR-0027.** После расторжения собственник фиксирует у
каждой позиции описи состояние, примечание и сумму ущерба, отправляет акт, а
арендатор подтверждает. Правка после отправки сбрасывает отправку; после
подтверждения состояние и денежный снимок неизменяемы. Сумма ущерба считается
в `Prisma.Decimal`, возврат депозита не опускается ниже нуля.

На `Lease` разделены неизменяемые `returnActDamageTotal`,
`returnActDepositReturn`, `returnActUncovered` и изменяемый
`returnActUncoveredRemaining`. Перенос из ADR-0025 сначала гасит этот остаток и
только затем увеличивает живой `depositReturnAmount`. Правка позиции,
подтверждение и финализация счёта первым действием транзакции блокируют строку
договора через `SELECT ... FOR UPDATE`.

Добавлен третий вид документа `return_act`. Предварительная версия считается
из текущих позиций, подтверждённая печатается только из неизменяемого снимка.
На `/leases/[id]` появилась секция акта возврата с двусторонними действиями,
сводкой и печатным превью; состав исходной описи в этом режиме не меняется.

Миграция `20260824151355_return_act` создана через `prisma migrate diff` и
применена через `prisma migrate deploy`. После остановки backend успешно
выполнен `prisma generate`, backend перезапущен с новым клиентом. Frontend
`:3001` не останавливался. Backend suite после исправления города документа —
**302/302** (26 наборов); backend
`tsc --noEmit` и `nest build`, frontend `tsc --noEmit` чисты.

Живой E2E по API Фазы 9 выполнен ревьюером, **32/32**: правка состояния на
активном договоре отвергается; `ok` с суммой ущерба отвергается; отправка при
незаполненной позиции отвергается; правка после отправки сбрасывает отметку
отправки; подтверждение без отправки и подтверждение собственником
отвергаются. Подтверждение арендатором вычитает ущерб 7000 из депозита 30000 и
записывает снимок; повторное подтверждение второй раз не вычитает, а правка
после подтверждения отвергается. Акт генерируется, помечен подтверждённым и
читается арендатором. При ущербе 25000 и депозите 10000 возврат равен нулю,
непокрытый ущерб и его остаток равны 15000, превышение напечатано в акте.
Пустая опись отправляется и подтверждается, депозит не меняется.
Throwaway-данные удалены.

**Браузерная проверка выполнена, накопленный долг за Фазы 5, 6, 8 и ADR-0022
закрыт.** На засеянных throwaway-данных, удалённых после осмотра, проверено
глазами: бланк договора с длинным кадастровым номером
`38:36:0000021:12345` и структурированным адресом не ломает вёрстку, строка
переносится штатно; список объектов группируется по городам; на карточке
объекта в подзаголовке виден кадастровый номер, работает диалог редактирования
со всеми полями адреса и подсказкой формата, timezone в форму справедливо не
вынесен. В услугах обе позиции из заявок помечены «из заявки» со статусами
«Выставлено» и «Ждёт выставления». На хабе показаний плашка «показания до»
стоит ровно за 5 дней до конца периода, пилюли «Показания внесены» и «Внесите
до» различаются цветом и иконкой, кнопки «Копировать» на месте. На экране
счетов отрицательная строка вычета за счёт собственника выводится акцентным
цветом с пометкой плательщика. На карточке договора секция персональных данных
показывает обе стороны с датами.

**Браузерный долг закрыт целиком, незакрытых пунктов не осталось.** Экран акта
возврата Фазы 9 целиком пройден на throwaway-данных: расторгнутый договор и
опись из трёх позиций — повреждено на 7500, норма, отсутствует на 15000.
Проверено: у арендатора все поля состояния заблокированы и доступна только
кнопка «Подтвердить акт»; до подтверждения показывается «Итого ущерб 22 500 ₽»,
а депозит остаётся 40 000 ₽; после нажатия пилюля сменилась на «Подтверждён
24.08.2026», депозит пересчитался в 17 500 ₽, кнопка исчезла и появилась
подсказка про устаревший документ. Throwaway-данные удалены.

Стенд: backend `:3000` и frontend `:3001` работают, база очищена от
throwaway-данных.

### Предыдущий закрытый этап — сессия 2026-08-21

**Фаза 8 закрыта по ADR-0026.** В `Property` добавлены nullable-поля
структурированного адреса и кадастровый номер; `address` оставлен
денормализованным кешем. Новые объекты создаются только по обязательной тройке
город/улица/дом — обе формы (`/onboarding` и `/properties`) переведены на этот
контракт. PATCH проверяет итоговое состояние после слияния и не пишет частичный
адрес в legacy-объект; неадресная правка сохраняет его прежнюю строку.

Миграция `20260821190000_structured_address` создана через `prisma migrate diff`
и применена через `prisma migrate deploy`, без бэкфилла. После неё выполнен
`prisma generate`. Договор подставляет кадастровый номер с прочерком при
отсутствии. Список объектов сортируется и группируется по городу; объекты без
города идут последней группой. В карточке появилась форма редактирования,
которая позволяет постепенно переносить legacy-адреса по подсказке старой
строки.

Backend suite — **265/265** (22 набора); backend `tsc --noEmit` и
`nest build`, frontend `tsc --noEmit` чисты. Локальная БД содержит все 12
миграций.

Живой E2E по API выполнен ревьюером, **31/31**: объект создаётся по полям,
а кеш `address` собирается как «г. Иркутск, ул. Зверева, д. 9Б, стр. 2,
эт. 3, кв. 15»; свободная строка `address` без тройки отвергается с 400;
отсутствие дома и город из одних пробелов дают 400. Кадастровые номера
`24:5:111213:1` и `38:36:000021:1234` принимаются, а `abc`,
`38-36-000021-1234` и `38:36:000021` отвергаются. У legacy-объекта правка
одной квартиры даёт 400 и ничего не записывает, правка площади проходит и
адрес остаётся прежним, а заполнение тройки пересобирает кеш. У объекта с
заполненной тройкой правка квартиры пересобирает кеш. Очистка строения через
`null` и этажа пробелами убирает их из кеша; кадастровый номер стирается пустой
строкой. В договоре кадастровый номер подставляется, а без него остаётся
прочерк. Список объектов отсортирован по городу, объекты без города в конце.
Throwaway-данные удалены.

Стенд: backend `:3000` работает на новом Prisma Client после миграции,
frontend `:3001` не перезапускался. Frontend `tsc --noEmit` чист, экраны
`/properties`, `/onboarding` и `/dashboard` отвечают 200.

Накопленный браузерный долг Фазы 8 закрыт ревьюером 2026-08-24; результаты
зафиксированы в разделе текущей сессии выше.

**Фаза 6 закрыта по ADR-0025.** Согласованная сумма заявки теперь создаёт
`one_time`-услугу, связанную с заявкой, а в счёт уходит только на
`resolved`. Условные `updateMany` захватывают заявку при согласовании,
услугу при выставлении и счёт при финализации. Старый прямой путь
`addSettlementLine` удалён; ручные разовые услуги автоматически
выставляются при создании черновика.

`tenant` даёт положительную полную сумму, `split` — `round2(price / 2)`,
`owner` — отрицательный вычет. При финализации строки перечитываются внутри
транзакции. Отрицательный остаток переносится связанными строками в следующий
счёт, а при расторжении доводит последний счёт до нуля и увеличивает
перечитанный `depositReturnAmount`. Пеня, просрочка и заявление об оплате для
итога `<= 0` отключены.

Миграция `20260821160000_service_settlement` создана через
`prisma migrate diff` и применена через `prisma migrate deploy`. Первый
`prisma generate` не смог заменить query engine DLL из-за `EPERM`: её держал
работающий backend. Ревьюер остановил backend, успешно повторил
`prisma generate` и запустил backend заново.

Frontend показывает ожидание закрытия заявки, факт выставления/вычета,
источник разовой услуги и дату выставления; согласованная сумма `0` видна,
отрицательные строки читаются как вычеты, нулевой итог явно подписан
«К оплате: 0 ₽».

Backend suite — **244/244** (20 наборов); backend `tsc --noEmit` и
`nest build`, frontend `tsc --noEmit` чисты. Живой E2E по API выполнен
ревьюером, **24/24**: согласование создаёт ровно одну разовую услугу
и не создаёт строку счёта; повторное подтверждение второй услуги не создаёт;
`resolved` выставляет услугу (`+5000` при плательщике-арендаторе), повторный
`resolved` не дублирует строку; пополам даёт `+1500`, плательщик-собственник —
строку `-2000`. При вычете больше счёта черновик уходит в минус; после
финализации итог финального счёта равен нулю, а в следующем счёте появляется
строка `-15000`, причём обе строки переноса ссылаются друг на друга через
`sourceRefId`. При расторжении остаток ушёл в депозит (`10000` задано, стало `15000`),
отрицательных финальных счетов не осталось; ручная разовая услуга выставилась
в новый счёт и помечена `billedAt`; правка и удаление услуги из заявки
отклоняются с `409`. Throwaway-данные удалены.

Стенд: backend `:3000` ревьюером перезапущен, frontend `:3001` не перезапускался.

**Известный дефект, кандидат в самостоятельную задачу:** расторжение сначала
фиксирует статус договора и заявки, а затем вызывает биллинг вне этой
транзакции. Если биллинг упадёт, договор останется расторгнутым с незакрытым
черновиком и автоматически не восстановится. Сбой пишется в лог с `leaseId`
и `billId`; простое повторение небезопасно из-за неидемпотентной пропорции
аренды. Финальный счёт при расторжении также сознательно закрывается без
гарда показаний: недостающую коммуналку стороны учитывают при взаимозачёте
возврата депозита.

**Фаза 5 закрыта по ADR-0024.** Срок показаний считается как
`periodEnd − 5 календарных дней` по UTC-суткам. Обязанность больше не
ищет показание в окне дат: единый гард и API-статус проверяют строку
`utility / meter_reading` конкретного открытого счёта. Поэтому поздняя
подача расшивает зависший счёт и не может тем же показанием закрыть
следующий период.

На `Bill` добавлены независимые at-most-once отметки просрочки срока и
невозможности расчёта; этим устранён почасовой спам `readings_missing`.
Показание и строка расхода создаются одной транзакцией, будущая дата
запрещена, повторные показания в периоде разрешены. Миграция
`20260821120000_readings_alerts` применена через `migrate deploy` после
двух успешных SQL-проверок целостности.

Frontend показывает срок и статусы на хабе и «Сегодня», копирует голое
нормализованное число с хаба и истории. Backend suite — **214/214**;
backend/frontend `tsc --noEmit` и backend `nest build` чисты.

Живой E2E по API выполнен ревьюером, **26/26**: хаб сам создаёт черновик
(`ensureCurrentDraft`) и отдаёт `readingsDueDate` и `readingsDaysLeft`; дедлайн
на 5 дней раньше конца периода; статус `due` до дедлайна, `overdue` после
(`readingsDaysLeft = -3` при сдвиге на 3 суток); при зависшем счёте хаб
показывает период зависшего счёта, а не следующий календарный; позднее
показание кладёт строку расхода 750 в зависший счёт и переводит статус в
`submitted`; после финализации зависшего счёта следующий период снова требует
показание, то есть каскада нет и чужой строки расхода в новом счёте нет;
повторное показание в периоде даёт суммарно 250 без двойного начисления;
будущая `readingDate` отвергается с 400; показаний без строки расхода в базе нет.

Отдельная регрессия на посторонние потоки выполнена, **9/9**: новый уникальный
индекс `bills(leaseId, periodStart)` не мешает ручной финализации, следующий
черновик создаётся ровно один, заявка на расторжение и её финализация проходят
без ошибок, договор переходит в `terminated`, дублей счетов нет, хаб
расторгнутого договора отвечает 200. Throwaway-данные удалены после обоих
прогонов.

Накопленный браузерный долг Фаз 5–6 закрыт ревьюером 2026-08-24; результаты
зафиксированы в разделе текущей сессии выше.

### Предыдущая сессия — 2026-08-20 (вторая половина дня)

Контур ADR-0021 завершён ADR-0022: backend фиксирует дату и версию
согласия отдельно от шифрованных ПДн, публично отдаёт текст политики
вместе с хешированной версией и отклоняет сохранение, если пользователь
видел другую редакцию. Текущая версия имеет суффикс `-draft`: юридический
текст ещё готовится, поэтому реальным пользователям механизм не
открывать, на стенде использовать только синтетические данные.

Frontend готов: `/leases/[id]/party-info` доступен обеим сторонам для
своей записи, сохраняет паспорт/дату рождения/адрес/телефон, повторно
просит согласие после смены редакции и становится read-only на
`terminated`. На карточке договора видны статусы обеих сторон без чужих
ПДн и подсказка о перегенерации устаревшего текста; на «Сегодня» есть
напоминание о своих незаполненных данных. `/legal/privacy` публичен,
ссылки добавлены на вход и регистрацию.

Backend дополнительно не записывает неизменившийся payload, уведомляет
контрагента только при реальной правке, печатает дату рождения и не
подставляет в договор строки без зафиксированного согласия. Миграция
`20260820130000_party_info_consent` применена локально через
`migrate deploy`.

Живой E2E по API выполнен ревьюером, **36/36**: публичная политика доступна
без токена; `PUT` без согласия возвращает 400, с чужой `policyVersion` — 409,
с верной версией — 200; повторный `PUT` теми же данными не двигает
`updatedAt`; статус обеих сторон не раскрывает ПДн; посторонний получает 404
и на статус, и на чужие данные; уведомление `party_info_submitted` доставлено;
в сгенерированном договоре есть ФИО, паспорта, адреса, телефоны и даты рождения
обеих сторон, даты рождения имеют формат `ДД.ММ.ГГГГ`; отвергаются серия из
3 цифр, номер из 5 цифр, `2025-02-31`, дата в будущем, несовершеннолетний и
телефон не по маске; очистка телефона убирает его и из договора; `PUT` на
`terminated` возвращает 403, чтение при этом работает. Throwaway-данные
удалены.

Накопленный браузерный долг ADR-0022 закрыт ревьюером 2026-08-24; результаты
зафиксированы в разделе текущей сессии выше.

На тот момент следующим шагом была Фаза 5; она закрыта 2026-08-21.
OCR по-прежнему не начинать без отдельного разговора.

### Предыдущая сессия того же дня (2026-08-20, первая половина)

Закрыты **Фаза 3** (ADR-0019) и **Фаза 4** (ADR-0020).

Фаза 3 — денежный цикл: модель `PaymentProof`, `claim-paid` стал
multipart с обязательным чеком (ломающее изменение эндпоинта,
намеренное), замена чека до подтверждения удаляет старый файл, чек виден
обеим сторонам и после `paid`; реквизиты арендодателя
(`User.payoutPhone`/`payoutBankName`/`payoutNote`, `GET`/`PATCH /auth/me`,
`GET /leases/:id/payout-details`) и блок «Куда платить» с копированием на
экране счетов (`frontend/lib/clipboard.ts`).

Фаза 4 — приглашение и «Сегодня»: приглашение видно отправителю,
переотправка на исправленный адрес отзывает прошлое (`cancelled`), есть
явный отзыв с возвратом договора в `draft`; договор отдаётся как
`LeaseView` (объект + обе стороны + приглашение только арендодателю),
из-за чего у арендатора вместо «Договор» появился адрес объекта; пункт
«Приглашения» в навигации виден, только пока есть приглашения
(`frontend/lib/events.ts` обновляет его сразу после ответа).

Проверки на момент остановки: бэкенд **145/145**, `tsc --noEmit` чист на
бэкенде и фронтенде, `next build` проходит; оба инкремента пройдены
живьём в браузере и по API, throwaway-данные и файлы удалены.

**Следующий шаг — Фаза 5** (показания как рабочий инструмент), нужен
короткий ADR + дизайн.

**Ждут человека, не кода (не начинать самостоятельно):** разговор про OCR
(чекпойнт ниже), ответ CEO по Фазе 7 и вопрос про отдельное поле под
номер банковской карты (`PRODUCT_QUESTIONS.md`, 7.5).

### Предыдущая сессия (2026-08-19, вечер)

Закрыты **Фаза 2** (два точечных бага: `isActive`-фильтр в
`billing.service.ts`, join договора в `listMyInvitations`) и **фронт
Фазы 1** (`components/InventoryEditor.tsx`, шаг «Опись имущества» в
мастере, секции описи и Приложения №1 на карточке договора).

### Как этот план вообще появился

Тестировщик прогнал MVP end-to-end и прислал развёрнутый отчёт по каждому
экрану. Он разобран и превращён в фазированный план улучшений (баги vs
UX vs продуктовые решения, требующие ADR) — план утверждён и **выполняется
по фазам, начиная с Фазы 0**. Отчёт тестировщика целиком не переносился в
docs — сохранён только результат разбора (этот файл + ADR-0017/0018).

**Вводная про ПДн менялась дважды за 2026-08-19/20 — читайте по датам.**
Сначала (2026-08-19, утро) — «сервис сознательно не заходит в периметр
152-ФЗ, ничего не вносится» (ADR-0017). При подготовке этого решения
всплыло, что похожее уже принималось раньше: `TenantInfo` построен
2026-07-22, удалён 2026-07-23 по фидбэку клиента — без собственного ADR,
с расхождением в документации (ROADMAP.md показывал «✓ готово» уже после
удаления), исправлено тогда же. Затем (2026-08-20, после разговора
клиента с партнёром) — решение пересмотрено на противоположное:
собираем ПДн обеих сторон, делаем комплаенс правильно (RU-хостинг,
оператор ИП/самозанятый, ретеншен как реальная задача, а не только
описание) — см. ADR-0021 и раздел «Где остановились» выше.

### Фазы плана (Фазы 0–4 сделаны, дальше по порядку)

- **Фаза 0 (ADR-0017, 2026-08-19 утро) — отменена ADR-0021 в тот же
  день позже.** Изначально убирала автоподстановку ФИО/ПДн из договора.
  Отменена целиком: ФИО и ПДн (если внесены) снова подставляются — см.
  «Фаза 0.1» ниже. Оставлено в списке для истории, не как активный
  статус.
- **Фаза 0.1 (готово 2026-08-20, ADR-0021, backend):** ПДн сторон
  договора — обе стороны, `LeasePartyInfo` + `CryptoService`, ретеншен
  реальной фоновой задачей. Подробности — «Где остановились» вверху
  файла. **Фронт не сделан** — следующий шаг.
- **Фаза 1 (готово целиком, backend + фронт; ADR-0018):** новая модель
  `LeaseInventoryItem` (опись имущества: тип техники/бренд/модель/
  количество, без ПДн) + `LeaseDocument.kind` (`contract`|`handover_act`,
  версионируется раздельно) + шаблон-приложение «Акт приёма-передачи
  имущества» + эндпоинты `leases/:leaseId/inventory-items` (CRUD) и
  `leases/:leaseId/document/handover-act` (генерация/чтение).
  Фронт (2026-08-19): общий компонент `components/InventoryEditor.tsx`
  — шаг 4 «Опись имущества» в мастере (`/onboarding`, теперь 5 шагов) и
  секции «Опись имущества» + «Приложение №1 — акт приёма-передачи» на
  карточке договора (`/leases/[id]`). Правка описи — собственнику и
  только в черновике; генерация акта — собственнику в любом статусе;
  арендатор видит опись и акт на чтение.
- **Фаза 2 (готово 2026-08-19, без ADR — правки не меняли решений):** два
  точечных бага.
  1. `billing.service.ts`, `firstMeterWithoutReading` — не фильтровал
     счётчики по `isActive`; отключённый счётчик требовал показание,
     которое `MeterReadingsService.create` больше не принимает
     (ADR-0014) → счёт для объекта нельзя было сформировать никогда.
     Добавлен `isActive: true` в `where` ✓.
  2. `leases.service.ts`, `listMyInvitations` — не джойнил договор,
     фронту нечего было показать в карточке приглашения, кроме email
     самого приглашённого. Добавлен `include` с `landlord`
     (`fullName`/`email`) и `property` (`address`) + условия договора
     (срок, аренда), тип ответа — `InvitationView`; карточка
     `/invitations` и действие на «Сегодня» переписаны под него ✓.
  Оба фикса проверены на живом стенде (см. `docs/CHANGELOG.md`).
- **Фаза 3 (готово 2026-08-20, ADR-0019):** денежный цикл замкнут.
  `PaymentProof` — чек обязателен при заявлении оплаты
  (`claim-paid` стал multipart), заменяется до подтверждения, виден обеим
  сторонам и после `paid`; файл через `StorageProvider` (ADR-0007) ✓.
  Реквизиты арендодателя — `User.payoutPhone`/`payoutBankName`/
  `payoutNote`, `PATCH /auth/me` + `GET /leases/:id/payout-details`, блок
  «Куда платить» с копированием на экране счетов ✓. Поля под номер карты
  сознательно нет — вопрос клиенту в `PRODUCT_QUESTIONS.md` 7.5.
  Общий помощник копирования — `frontend/lib/clipboard.ts` (пригодится
  Фазе 5).
- **Фаза 4 (готово 2026-08-20, ADR-0020):** приглашение и «Сегодня».
  Приглашение видно отправителю (кому и когда), переотправляется на
  исправленный адрес одним действием (прошлое → `cancelled`) и
  отзывается (`DELETE /leases/:id/invitation`, договор обратно в
  `draft`) ✓. Договор отдаётся как `LeaseView` — объект + обе стороны
  (+ приглашение только арендодателю); заодно у арендатора вместо
  «Договор» появился адрес объекта на карточке и в «Сегодня» ✓. Пункт
  «Приглашения» в навигации показывается, только пока есть неотвеченные
  приглашения; после «Принять» исчезает сразу (`frontend/lib/events.ts`) ✓.
- **Фаза 5 (готово 2026-08-21, ADR-0024):** дедлайн показаний за пять
  календарных дней до конца периода, статусы на хабе и «Сегодня»,
  at-most-once уведомления о просрочке и невозможности расчёта, копирование
  голого числа на хабе и в истории ✓. Обязанность привязана к строке
  расхода конкретного счёта; поздняя подача расшивает зависший счёт без
  каскада на следующий. Показание и начисление транзакционны ✓.
- **Фаза 6 (готово 2026-08-21, ADR-0025):** согласованная заявка создаёт
  защищённую `one_time`-услугу, `resolved` выставляет её ровно один раз;
  ручные разовые услуги также работают. Знак зависит от плательщика,
  отрицательный остаток переносится в следующий счёт или возврат депозита ✓.
- **Фаза 7 (не начата, ждёт явного да/нет от CEO, не техническая
  развилка):** переименовать «адрес» объекта в «название» + разбить на
  город/улицу/дом/строение/этаж/квартиру ради будущей карты. Отменяет уже
  закрытый пункт 14.1 в `PRODUCT_QUESTIONS.md` («карты — не MVP») — не
  начинать молча, нужен явный пересмотр.

### Фазы 8–12 (новые направления, добавлены 2026-08-19 по итогам мозгового
штурма с клиентом; Фаза 8 закрыта)

Важно: Фаза 8 **не то же самое**, что Фаза 7. Фаза 7 — сама визуальная
карта, упирается в закрытый вопрос 14.1, ждёт CEO. Фаза 8 — только
структура данных под адрес, без самой карты и без переименования поля в
«название» — не пересматривает 14.1 и можно начинать сразу.

- **Фаза 8 (готово 2026-08-21, ADR-0026):** структурированные поля адреса
  стали источником правды, `Property.address` сохранён совместимым кешем,
  кадастровый номер подключён к договору ✓. Обе формы создания переведены на
  обязательную тройку; редактирование постепенно мигрирует legacy-объекты без
  автоматического разбора строк. Сортировка и группировка по городу готовы ✓.
- **Фаза 9 (готово 2026-08-24, ADR-0027):** двусторонний акт возврата
  фиксирует состояние описи и уменьшает возврат депозита на согласованный
  ущерб ✓. Неизменяемый денежный снимок отделён от остатка непокрытого ущерба;
  три пересекающихся потока защищены блокировкой строки договора. Третий вид
  документа и секция на карточке расторгнутого договора готовы ✓.
- **Фаза 10 (готово 2026-08-24, без ADR и миграции):** история
  арендаторов на карточке объекта. Данные уже есть (`Lease[]` на
  `Property`, доступ landlord сохраняется после смены арендатора —
  решено в `docs/ARCHITECTURE.md`). Нужен эндпоинт вида `GET
  properties/:id/lease-history` (landlord-only): прошлые договоры + email
  арендатора + пунктуальность оплат (агрегат по `Bill`/`Payment`, по
  аналогии с расчётом `daysOverdue` в `reports.service.ts`, но
  исторически по каждому завершённому Lease, а не только по текущим
  просрочкам). **Важно не спутать с ADR-0017:** тот запрещает печатать
  ПДн *в договоре*, а не показывать landlord'у, кто у него арендовал —
  это его собственная бизнес-история, email арендатора он и так знает.
  Фронтенд — простой таймлайн на `/properties/[id]` ✓. API ограничен
  завершёнными договорами этого landlord, пунктуальность агрегирует только
  финальные счета; текущий договор и история разделены ✓.
- **Фаза 11 (не начата, нужен ADR — новая продуктовая поверхность):**
  финансовая проекция при заключении договора. Landlord: интерактивный
  график/план дохода на срок договора (из `rentAmount`/`paymentDay`
  `Lease` — сколько накопится за год при соблюдении графика платежей;
  неплохо бы план-факт поверх уже реальных `Bill`/`Payment`). Tenant: та
  же сетка платежей, но как «сколько потрачено», плюс интерактивный
  калькулятор «а если бы эти же деньги каждый месяц клались на вклад под
  N% годовых» (сложный процент по ежемесячным взносам, ставка —
  **настраиваемый ползунок**, дефолт иллюстративный ~10%, не хардкодить
  как факт реальной ставки банка). Чисто вычисляемая фича — новая
  персистентная сущность не нужна, считается на лету по данным `Lease`
  (+ `Bill`/`Payment` для плана-факта у landlord'а).
  **Копирайт-нюанс:** явно подписать как иллюстративный расчёт, не
  финансовая рекомендация конкретного продукта/банка — тема соседствует
  с персональными инвестиционными советами, которые сервису давать
  нельзя.
- **Фаза 12 (готово 2026-08-24, ADR-0028):** `/reports` показывает все
  объекты собственника портфелем с группировкой по городу, статусом аренды,
  доходом, долгом, заявками и деньгами на подходе ✓. Деньги объектов считаются
  в том же проходе, что общие итоги; знак услуги общий с биллингом. Чужие
  договоры отсекаются, активный договор выбирается по наибольшей `startDate`.

**Осознанно не в этом списке — OCR показаний (ADR-0008, сейчас
заглушка):** клиент попросил обсудить этот пункт отдельно, до включения
в план. **Не начинать без этого разговора.**

#### Чекпойнт разговора про OCR (2026-08-19, обсуждение в процессе — не решено)

Клиент беспокоится, что размытые/некачественные фото со счётчиков —
большой UX/бизнес-риск (неверно распознанное показание → неверный счёт).
Разбор в чате дошёл до этой точки, продолжение — на следующей сессии:

- **Существующая защита уже есть и не обсуждается** — `MeterOcrProvider.recognize()`
  возвращает `number | null`, и по контракту интерфейса (и по ADR-0008)
  результат **всегда** редактируется/подтверждается пользователем перед
  сохранением, никогда не применяется вслепую. Это не меняется независимо
  от выбора движка ниже.
- **Разобраны 3 варианта движка**, с честными компромиссами:
  1. **Tesseract** (текущий выбор ADR-0008) — общий OCR, не заточен под
     цифры на табло; на размытом фото скорее всего просто ничего не
     распознает (безопасный отказ, не уверенная ошибка). Бесплатно,
     офлайн.
  2. **Облачный OCR (Yandex Vision)** — точнее на плохих фото, но на
     действительно плохом фото может увереннее ошибиться, чем Tesseract.
     Платно, и требует отправки фото на сторонний сервис. **Важное
     обновление (2026-08-20, после ADR-0021):** причина, по которой
     ADR-0008 в июле выбрал Tesseract («вопрос хостинга/локализации не
     решён»), по сути снята — сервис теперь идёт на RU-хостинг и уже
     решил обрабатывать более чувствительные данные (паспорта, ADR-0021).
     Показания счётчика — не персональные данные, но раз компания уже
     взяла курс на серьёзный комплаенс, отправка фото во внешний OCR
     стала намного менее спорной, чем в июле. Стоит поднять это явно в
     следующем разговоре про OCR — вывод «сначала бесплатный спайк на
     Tesseract» ниже может уже не быть единственным разумным путём.
  3. **Vision-модель (мультимодальная LLM)** — качественно другое
     поведение: устойчивее к размытию за счёт контекстного понимания фото,
     и — это здесь ключевое — может честно ответить «не могу разобрать»
     вместо галлюцинации цифры. Дороже за вызов, тот же вопрос внешней
     передачи данных, что и у варианта 2.
- **Важный факт, обнаруженный в разборе:** ADR-0008 ещё в июле написал
  «если точность Tesseract на реальных фото окажется неудовлетворительной
  — переходить на Yandex Vision», но этот тест ни разу не проводился —
  до сих пор в проде `MockMeterOcrProvider`, всегда возвращающий `null`.
  Решение «Tesseract недостаточно точен» пока не подтверждено данными.
- **Предложение (озвучено, ответ клиента не получен):** (1) дешёвый
  client-side blur-check при съёмке (дисперсия Лапласиана — просить
  переснять сразу, без похода на сервер) — защищает независимо от
  движка и заодно защищает само фото как доказательство показания; (2)
  быстрый бесплатный спайк — прогнать Tesseract на нескольких настоящих
  фото счётчиков (включая заведомо плохие) и посмотреть на реальную
  точность, прежде чем платить за облако и открывать вопрос передачи
  фото наружу.
- **Открытый вопрос для следующей сессии:** делать ли сначала
  blur-check + спайк на Tesseract, или клиент хочет сразу закладываться
  на платный OCR/vision-модель, не тратя время на бесплатный вариант.
  **Не начинать реализацию OCR, пока это не решено явно.**

## Что уже сделано

### Backend (`backend/`) — NestJS, модульный монолит, Prisma + PostgreSQL

Phase 1 роадмапа (`docs/ROADMAP.md`) функционально закрыт целиком, плюс
Фазы 0–1 плана качества сверху. Модули:

- **auth** — регистрация/вход, JWT access+refresh, argon2, `SuperAdminGuard`.
- **properties / services / meters** — объект + каталог услуг + счётчики
  + показания (фото + OCR-заглушка), `isActive`/`initialReading`/
  `serialNumber` (ADR-0014), экран-хаб показаний для tenant (ADR-0015).
- **leases** — договор (draft→sent→active→terminated), `Invitation`,
  `LeaseSignedScan` (активация по 2 сканам), `LeaseDocument` (текст
  договора РФ, **ПДн сторон из `LeasePartyInfo` — ADR-0021**, + Приложение
  №1 «Акт приёма-передачи имущества» — **ADR-0018**), `LeaseInventoryItem`.
- **party-info / legal** — `LeasePartyInfo` (обе стороны, по одной записи
  на роль), `CryptoService`/`CryptoModule` (AES-256-GCM), дата и версия
  согласия, публичная хеш-версионируемая политика, статусы сторон,
  уведомление контрагента и ретеншен 3 года фоновой задачей
  (ADR-0021/0022). **Если в старых заметках/памяти встретится
  «TenantInfo не будет реализована» или «сервис не собирает ПДн» —
  это устарело**, актуальное решение прямо противоположное.
- **billing** — `Bill` (draft/final), статусы оплаты (ADR-0012), пени
  (ленивый расчёт), прощение, планировщик периодов (`@nestjs/schedule`,
  ADR-0013), каскад напоминаний, чек об оплате `PaymentProof` (ADR-0019).
  Гард финализации по показаниям считает только активные счётчики
  (Фаза 2, п.1 — исправлено 2026-08-19).
- **maintenance** — заявки + двустороннее согласование суммы → счёт.
- **messages** — чат по договору, вложения, удаление (SuperAdmin).
- **reports** — сводка собственника (доходы/просрочки по оплате; **нет**
  отдельного статуса просрочки по показаниям — см. Фаза 5).
- **notifications** — `NotificationChannel` (консоль-заглушка) + persist + триггеры.
- **termination** — расторжение (30 дней, финализирует landlord, пропорция).
- **storage / ocr** — `StorageProvider` (локальная ФС, ADR-0007), OCR-заглушка.

Тесты: `npm test` в `backend/` — **187/187 зелёные** (Prisma замокан, БД не
нужна). `npx tsc --noEmit` и `npx nest build` — чистые.

### Frontend (`frontend/`) — Next.js (App Router, TS)

Экраны: `/login`, `/register`, `/onboarding`, `/dashboard` (главная,
«Сегодня»), `/properties`, `/properties/[id]` (счётчики/услуги),
`/leases`, `/leases/[id]` (текст/сканы/активация/отправка),
`/leases/[id]/meters` (хаб показаний, ADR-0015) +
`/leases/[id]/meters/[meterId]/history`, `/leases/[id]/bills`,
`/leases/[id]/chat`, `/leases/[id]/requests`, `/leases/[id]/termination`,
`/leases/[id]/party-info`, `/legal/privacy`, `/invitations`, `/reports`,
`/notifications`.

Опись имущества и акт приёма-передачи (ADR-0018) живут в общем компоненте
`components/InventoryEditor.tsx`: шаг 4 мастера `/onboarding` и две секции
на `/leases/[id]`. Нет `tenant-info` роута — удалён вместе с сущностью
2026-07-23, это ожидаемо; вместо него используется симметричный для обеих
сторон `/leases/[id]/party-info`.

## Как запустить / проверить

```bash
# инфраструктура (уже поднята на этой машине)
cd backend && docker compose up -d              # Postgres 16 + Redis 7, :5432/:6379

# backend
cd backend && npm install && npm test           # тесты без БД, 187/187
npx tsc --noEmit                                 # типы
npm run start:dev                                # :3000, нужен Postgres

# frontend
cd frontend && npm install && npm run dev -- -p 3001   # :3001
```

**После изменения `prisma/schema.prisma`** нужно остановить работающий
backend даже в watch-режиме, выполнить `prisma generate` и запустить backend заново.
Иначе query engine DLL может остаться заблокированной (`EPERM`), а процесс продолжит
работать со старым Prisma Client и старым поведением; любые живые проверки покажут
несуществующие ошибки.

**Важно:** `next build` и `next dev` используют общий каталог `.next`, поэтому
продакшен-сборку нельзя запускать поверх работающего dev-сервера. Если после
этого dev-сервер уже отдаёт 500 (например, `Cannot find module ./819.js`),
остановить его, удалить `frontend/.next` и запустить заново.

**Как проверять живьём** (этим проверялись Фаза 2 и фронт Фазы 1):
регистрировать throwaway-аккаунты прямо через API (`POST /api/auth/register`),
гонять сценарий скриптом (Python + `urllib` надёжнее, чем `curl` из Git Bash
— тот бьёт UTF-8 в аргументах), а в браузере логиниться не через форму, а
складывая токены в `localStorage` (`softrent.accessToken` /
`softrent.refreshToken`) и переходя на нужный экран. После проверки —
удалять за собой: `DELETE FROM properties WHERE "ownerId" IN (SELECT id
FROM users WHERE email LIKE '<префикс>%');` затем те же `users` (остальное
уходит каскадом), плюс каталог загрузок `backend/uploads/leases/<leaseId>`,
если загружались сканы.

Локальная БД мигрирована по всем 13 миграциям в `prisma/migrations/`
включительно; последняя —
`20260824151355_return_act` (ADR-0027), применена через
`migrate deploy`. Новую миграцию в этом окружении создавать через
`prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel prisma/schema.prisma --script`
+ вручную положить результат в `prisma/migrations/<timestamp>_name/migration.sql`
+ `prisma migrate deploy` — `prisma migrate dev` не работает
(`Prisma Migrate has detected that the environment is non-interactive`).

## Редизайн (ADR-0023) — закрыт, правила дизайна остаются действующими

**Редизайн завершён и влит.** `main` и `redesign/stitch` указывают на один
коммит `79cd00e`; заморозка фронтовых фаз снята. Реализованы Tailwind 3.4 +
слой токенов на CSS custom properties +
примитивы shadcn/ui в `frontend/components/ui/`, источник истины по
дизайну — проект в Stitch (MCP подключён у владельца продукта в среде
WSL). Обоснование, инварианты и разбор отклонённых вариантов —
`docs/adr/0023-visual-redesign-tailwind-stitch.md`, читать целиком перед
любой правкой фронта. Пошаговый бриф исполняющей сессии (`docs/REDESIGN_BRIEF.md`) удалён
после выполнения — он был одноразовым.

Ключевое для планирования: **редизайн идёт ДО Фаз 5–12, а не после.**
Почти каждая из этих фаз тянет за собой фронт (5 — экран истории
показаний, 10 — таймлайн объекта, 11 — график дохода и калькулятор,
12 — портфельный `/reports`). Если делать редизайн после, каждый такой
экран верстается дважды.

**Режим работы — соло и автономно** (решение владельца, 2026-08-20).
Параллельных сессий на время редизайна нет: фронт целиком ведёт одна
сессия в WSL, она же принимала дизайнерские решения сама по критериям брифа
(«Направление дизайна») и прошла R1 → R4 подряд, без промежуточных
согласований. Останавливается один раз — когда
все 19 экранов перерисованы, перенесены в код и проверены живьём.
Промежуточных согласований владелец сознательно не даёт; вместо них —
записи в блоке этапов ниже, чтобы можно было заглянуть в любой момент.

На время редизайна backend не вёлся. Это ограничение снято после его
закрытия; Фаза 5 уже выполнена поверх новой системы.

**Инварианты, нарушение которых означает ошибку:**

- `frontend/lib/*` (17 модулей — api, auth, billing, chat, leases, …)
  **не трогается**: это контракт с backend. Понадобилась правка там —
  выносить отдельной задачей, а не чинить по ходу редизайна.
- Продуктовое поведение экранов сохраняется. Редизайн — про визуал и
  структуру компонентов, не про логику. ADR-0016 (polling), ADR-0021/0022
  (ПДн и согласие) действуют без изменений.
- **Все npm-команды — только на стороне Windows.** В
  `frontend/node_modules` лежит `@next/swc-win32-x64-msvc`; `npm install`
  из WSL подменит нативные бинарники на linux-овые и сломает запуск на
  Windows. Сессия в WSL правит только исходники; дев-сервер поднимается
  на Windows (`localhost:3001`), из WSL он доступен — в `.wslconfig`
  включён `networkingMode=mirrored`.
- Направление правок дизайна: **Stitch → код**. Визуальная правка,
  начатая прямо в коде мимо Stitch, — расхождение, её надо вернуть в
  Stitch.

**Статус этапов.**

- **R0 — решение зафиксировано и среда подготовлена.** Готово 2026-08-20.
  ADR-0023, эта запись, `docs/CHANGELOG.md`, `docs/ROADMAP.md`,
  бриф этапа. Ветка `redesign/stitch` создана. На стороне
  Windows установлены пакеты (`tailwindcss@3.4`, `postcss`,
  `autoprefixer`, `tailwindcss-animate`, `class-variance-authority`,
  `clsx`, `tailwind-merge`, `lucide-react`, `sonner`, 18 примитивов
  `@radix-ui/react-*`) — сессии в WSL npm не нужен ни на одном этапе.
  Конфиги Tailwind ещё не написаны, это R2. Подняты backend
  (`localhost:3000`) и фронт (`localhost:3001`).
- **R1 — дизайн в Stitch, кода нет. Готово 2026-08-21.**
  Сделано 2026-08-20: browser-инструментов в среде нет вообще (ни
  Playwright, ни screenshot) — `stitch-extract-static-html` (снятие живых
  экранов с localhost:3001 через throwaway-аккаунты) технически
  невыполнима в этой сессии; вместо этого дизайн-система построена из
  кода (`stitch-extract-design-md` по `frontend/app/globals.css` +
  `components/ui.tsx` → `.stitch/DESIGN_BASELINE.md`) и продуктовых
  документов (`site-md` по IDEA/USER_FLOWS/MVP_SCOPE/DESIGN_SYSTEM →
  `.stitch/SITE.md`). Направление зафиксировано через `taste-design` +
  ручной синтез в `.stitch/DESIGN.md` (целевая система — вместо
  генерации нескольких сквозных вариантов и сравнения их в Stitch,
  решения по направлению приняты явно в тексте DESIGN.md и обоснованы
  там же — экономит бюджет генераций на 19×2 экрана впереди). Stitch
  project `9694110487873573027` ("SoftRent — визуальный редизайн"),
  design system создана и залочена (`assets/5695572871459613897`,
  Manrope/фиолетовый #8a4fe0→#5a2fb0/roundness TWELVE, полный текст
  правил в designMd). Сгенерировано 2 экрана из 3 опорных: `/login`
  mobile (screen `939dd5a363894cb1a53628016217fdb9`, фото-фон+frost,
  буквально по спеке DESIGN_SYSTEM.md) и `/dashboard` desktop landlord
  (screen `1f2f8d5862a348d5a6930deb7ea6bd06`, сайдбар+hero+двухколоночная
  сетка). Затем Stitch MCP отдавал 401 на любой вызов; владелец починил
  доступ, генерация продолжена в тот же день вечером.

  **Итог R1.** Все 19 маршрутов нарисованы в двух раскладках —
  desktop и mobile, 38 экранов, плюс два листа состояний. Реестр с
  актуальными screen id — `.stitch/SCREENS.md`, и это источник правды
  при переносе в код: правка через `edit_screens` иногда создаёт новый
  screen id вместо правки существующего, поэтому «какой id актуален»
  держится там, а не здесь. Там же — список мелких расхождений, которые
  дешевле починить в коде, чем перегенерацией.

  Роли распределены по экранам так, чтобы каждая была покрыта: у
  landlord — `/dashboard`, `/properties`, `/properties/[id]`,
  `/reports`, `/notifications`, `/leases/[id]`, `bills`, `requests`
  (desktop), `chat` (desktop), `termination` (desktop), `party-info`
  (desktop); у tenant — `meters`, `history`, `bills` (mobile),
  `requests` (mobile), `chat` (mobile), `party-info` (mobile),
  `termination` (mobile), `/invitations`.

  **Первый вариант `/dashboard` пришлось переделать** — Stitch выдал
  гигантский фиолетовый градиентный hero и англоязычную навигацию, то
  есть нарушил главное правило системы (фиолетовый только на CTA/
  ссылке/активной навигации/фокусе) и правило языка интерфейса. Обе
  версии v1 помечены в `.stitch/SCREENS.md` как заменённые. Вывод для
  оставшихся экранов: каждый сгенерированный экран смотреть глазами и
  сверять с §7 `.stitch/DESIGN.md`, а не принимать первый результат —
  модель регулярно тянет фиолетовый в декор и латиницу в подписи.

  **Решение по состояниям экранов (принято 2026-08-20, автономно).**
  Рисовать каждое из пяти состояний для каждого из 19 экранов — это
  95+ генераций, что несоразмерно ценности: состояния «загрузка» и
  «ошибка» структурно одинаковы во всём приложении. Поэтому рисуются:
  (1) скелетон загрузки и инлайновая ошибка — один раз, как образец
  паттерна, дальше повторяются в коде по образцу; (2) пустое состояние
  — отдельным экраном для каждого списочного маршрута, потому что текст
  и предлагаемое действие у них разные; (3) содержательные состояния
  («просрочено», «ждём вторую сторону», read-only на `terminated`) —
  там, где меняется состав экрана, а не только текст.
- **R2 — фундамент в коде. Готово 2026-08-21.** `tailwind.config.ts` +
  `postcss.config.js`; тема ссылается на CSS custom properties, значения
  живут только в `globals.css`. Слой токенов переписан на палитру
  `.stitch/DESIGN.md`, старые имена переменных оставлены алиасами — они
  уйдут вместе с последним классом, который их использует. Примитивы
  написаны руками в `components/ui/` (button, input, label, card,
  separator, skeleton, tabs, checkbox, dialog, select, table);
  индекс-файла в этой папке нет намеренно — рядом лежит старый
  `components/ui.tsx`, и импорт `@/components/ui` разрешался бы
  неоднозначно. Продуктовые компоненты свои: StatusPill, PageHeader,
  Section, List/Row, EmptyState, Field, Stat/StatRow, Fab, Segmented,
  LeaseTabs, AppShell, AuthLayout.

  Два решения по ходу, чтобы не наступить на них снова. Первое:
  прозрачность вынесена в отдельные токены (`--surface-icon`,
  `--overlay`, `--*-line`) — модификатор вида `/40` на цвете,
  заданном через `var()`, в Tailwind 3.4 не работает. Второе:
  контекст «поверх фото» (экраны входа) сделан подменой токенов в
  `.auth-shell`, а не вторым набором вариантов компонентов — те же
  `bg-surface-input` и `bg-accent` сами становятся светлыми на фото и
  фиолетовыми на десктопе.

  Оболочка подключается на самом экране (как раньше TopBar), а не в
  `app/layout.tsx`: пока часть экранов не перенесена, общий layout
  давал бы двойную навигацию, а экранам входа она не нужна вовсе.
- **R3 — перенос всех 19 экранов. Готово 2026-08-21.** Перенесены все
  маршруты, продуктовое поведение и тексты сохранены 1:1. Каждый экран
  просмотрен живьём в обеих ширинах (390×844 и 1440×900): приложение
  отрисовалось, переполнения по горизонтали нет, консоль чистая, кольцо
  фокуса при обходе с клавиатуры видно. Вместе с последним экраном
  удалены `components/ui.tsx` и `components/TopBar.tsx`, а `globals.css`
  сократился с 1544 до 253 строк: остались слой токенов, контекст
  экранов входа (`.auth-shell`) и базовые правила.

  **Что находила именно живая проверка**, хотя `tsc` и сборка CSS были
  чисты: невидимая первичная кнопка на фото (токен градиента
  подставляется в `background-image`, а простой цвет он не принимает);
  градиентный фон вместо плоского (в теме под именем `app` были заведены
  и цвет, и градиент); старые правила на голых тегах, красившие новые
  компоненты. Вывод на будущее: проверка типов и сборка стилей — не
  замена просмотру.

  **Осознанное расхождение с макетом:** на десктопном чате нет правой
  колонки с карточкой договора, которая есть в макете Stitch. Лента
  сообщений оставлена одной центральной колонкой; если колонка контекста
  понадобится, добавлять её в Stitch и в код вместе.

- **R4 — документация. Готово 2026-08-21.** `docs/DESIGN_SYSTEM.md`
  переписан из новой системы (раздел «Тон и стиль текста» перенесён без
  изменений, прежняя редакция осталась исторической справкой),
  `.claude/agents/designer.md` переведён на Stitch, `CHANGELOG` и
  `ROADMAP` обновлены. Удалены одноразовые артефакты этапа:
  `docs/REDESIGN_BRIEF.md`, `.stitch/SITE.md`, `.stitch/DESIGN_BASELINE.md`.
  Остаются `.stitch/DESIGN.md` (правила направления, на него ссылается
  дизайн-система) и `.stitch/SCREENS.md` (соответствие «маршрут → screen
  id», нужен при следующей работе в Stitch).

### Хвост, который нужно подчистить руками

Проверочные данные удалены частично: каталог загруженных сканов снесён,
а строки в БД из WSL удалить нечем — `psql` тут нет, а prisma-клиент
собран под Windows. Выполнить на стороне Windows:

```sql
DELETE FROM properties WHERE "ownerId" IN
  (SELECT id FROM users WHERE email LIKE 'qa-landlord-1787290725%'
                            OR email LIKE 'qa-tenant-1787290725%');
DELETE FROM users WHERE email LIKE 'qa-landlord-1787290725%'
                     OR email LIKE 'qa-tenant-1787290725%';
```

Пароль этих аккаунтов — `Test12345!`, данные синтетические, паспортных
данных в них нет.

## Рекомендуемый следующий шаг

**Фаза 10 закрыта.** Следующий незакрытый этап — **Фаза 11** (финансовая
проекция), но перед реализацией нужен продуктовый ADR: определить план-факт,
формулу иллюстративного вклада и дисклеймер.

Фазу 7 не начинать без ответа CEO по пересмотру 14.1. **OCR (ADR-0008)
сознательно не включён — сначала обсудить с клиентом в основном чате,
самостоятельно не начинать.**
